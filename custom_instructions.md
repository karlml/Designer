# Custom Instructions

## Component Development

- **Always update `demo.html`** when making any changes to components in `components.tsx`. The demo.html file contains CSS implementations of all components and must stay in sync with the React component library.
